version: "3"
services:

  repubikla:
    build: .
    image: imleo/repubikla:v2
    ports:
      - 8080:8080
    restart: always
    volumes:
      - ./client:/usr/src/app/client
      - ./views:/usr/src/app/views

